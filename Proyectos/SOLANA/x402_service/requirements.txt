# Dependencias mínimas del servicio x402 (todas instalables con pip)
# Nota: se pueden ejecutar pruebas sin instalar nada nuevo:
#   fastapi, uvicorn, pydantic, pynacl, httpx ya disponibles en conda.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pynacl>=1.5
httpx>=0.27
tenacity>=8.2
# Redis (opcional) — solo si se activa REDIS_URL; el store cae a memoria.
redis>=5.0
